/// <reference path="../../typings/jquery/jquery.d.ts" />
/// <reference path="../../typings/react/react.d.ts" />

//$(()=>{
    //  $.getJSON("/api/authors", adata => {
    //      adata.forEach(item => localData.authors.push(new Author(item.id, item.name)));
         
    //      $.getJSON("/api/books", bdata => bdata.forEach(item => {
    //         let match = localData.authors.filter(a => a.id === item.authorId);
    //         let author = match.length ? match[0] : null;    
    //         localData.books.push(new Book(item.id, item.title, author))
    //      }));
    //  });
     
    //  $.getJSON("/api/awards", awdata => awdata.forEach(item => 
    //     localData.awards.push(new Award(item.id, item.awardName))));
        
     
//});